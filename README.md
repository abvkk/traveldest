# Traveldest

## Schema used:

```sql
CREATE TABLE destinations (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    continent VARCHAR(50),
    activity VARCHAR(100),
    description TEXT
);
```
